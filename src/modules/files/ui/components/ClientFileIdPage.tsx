"use client";

interface ClientFileIdPageProps {
  params: {
    fileId: string;
    projectId: string;
    workspaceId: string;
  };
}

const ClientFileIdPage = ({ params }: ClientFileIdPageProps) => {
  return <div>ClientFileIdPage</div>;
};

export default ClientFileIdPage;
